# Entrega Semana 13 - Restaurante App

Este repositorio contiene la carpeta `restaurante_app` correspondiente a la actividad de la Semana 13.

Para ejecutar la aplicación, ingresar a `restaurante_app` y ejecutar:

```bash
python main.py
```
